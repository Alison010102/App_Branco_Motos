import React from "react";
import { View, Text, SafeAreaView } from "react-native";

export default function Acessorios() {
  return (
    <SafeAreaView style={{ flex: 1, backgroundColor: "#EDF2F4", justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 24, fontWeight: "bold", color: "#2B2D42" }}>Acessórios</Text>
    </SafeAreaView>
  );
}
